---
pageType: home
title: Embedding & Reranker
description: OpenAI 兼容的 Embeddings / Rerank 使用指南：base_url、api_key、模型列表与多语言调用示例。

hero:
  name: Embedding & Reranker
  text: 使用指南
  tagline: OpenAI 兼容：配置 base_url / api_key，快速接入 Embeddings 与 Rerank
  actions:
    - theme: brand
      text: 快速开始
      link: /guide/quickstart
    - theme: alt
      text: API 手册
      link: /api/embeddings

features:
  - title: OpenAI 兼容
    details: SDK / HTTP 调用方式与 OpenAI 一致，只需替换 base_url 与 api_key。
  - title: 多语言示例
    details: 提供 Python / Bun / cURL 等调用示例，便于快速复制。
  - title: 场景集成
    details: 覆盖 KiloCode、RAGFlow、AstrBot 知识库等常见接入场景。
---

## Base URL

```text
https://router.tumuer.me/v1
```

注意：这里已包含 `/v1`，完整接口为 `https://router.tumuer.me/v1/embeddings`。

## 从这里开始

- 用户指南：`/guide/`（快速开始、模型列表、代码示例）
- 场景示例：`/scenarios/`（KiloCode、RAGFlow、AstrBot 知识库）
- API 手册：`/api/`（创建嵌入请求、创建重排序请求）
